import { Box, Typography } from '@mui/material';

const Jobs = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Jobs
      </Typography>
      <Typography variant="body1">
        Manage job listings and descriptions
      </Typography>
    </Box>
  );
};

export default Jobs; 